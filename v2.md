# faucet 水龙头 v2

> faucet for SDAG testnet


```sql
CREATE TABLE `all` (
	`id`	INTEGER PRIMARY KEY AUTOINCREMENT,
	`reg-time`	INTEGER,
	`pay-time`	INTEGER,
	`finish-time`	INTEGER,
	`amount`	INTEGER,
	`from`	TEXT,
	`to`	TEXT
);
```

